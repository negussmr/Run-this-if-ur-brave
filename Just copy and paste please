<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>The Ultimate Troll Page™</title>
<style>
  :root{
    --bg1: #0f172a;
    --bg2: #ff4d6d;
    --glass: rgba(255,255,255,0.06);
    --neon: rgba(255,255,255,0.12);
  }

  html,body{
    height:100%;
    margin:0;
    font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
    background: radial-gradient(circle at 20% 20%, #001234 0%, #071033 20%, #1b0b2b 45%, #2b0b0b 100%);
    overflow:hidden;
    color:#fff;
  }

  /* big neon header */
  header{
    position:fixed;
    left:50%;
    transform:translateX(-50%);
    top:20px;
    z-index:10000;
    text-align:center;
    pointer-events:none;
  }
  h1{
    margin:0;
    font-size:44px;
    letter-spacing:2px;
    text-shadow:
      0 0 6px rgba(255,255,255,.08),
      0 0 12px rgba(255,80,80,.12),
      0 0 18px rgba(255,160,0,.06);
    opacity:.95;
  }
  p.subtitle{
    margin:4px 0 0;
    font-size:12px;
    color:rgba(255,255,255,0.7);
    pointer-events: none;
  }

  /* main stage for chaos */
  #stage{
    position:absolute;
    inset:0;
    display:flex;
    align-items:center;
    justify-content:center;
    pointer-events:auto;
  }

  /* center card */
  .card{
    width:720px;
    max-width:92%;
    min-height:420px;
    background:linear-gradient(135deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
    border-radius:18px;
    box-shadow: 0 12px 40px rgba(0,0,0,0.6);
    backdrop-filter: blur(6px) saturate(140%);
    border:1px solid rgba(255,255,255,0.03);
    padding:22px;
    position:relative;
    overflow:hidden;
  }

  /* fake toolbar */
  .toolbar{
    display:flex;
    gap:8px;
    align-items:center;
    margin-bottom:12px;
  }
  .dot{ width:12px; height:12px; border-radius:50%; }
  .dot.r{ background:#ff5f56; }
  .dot.y{ background:#ffbd2e; }
  .dot.g{ background:#27c93f; }

  /* large marquee text */
  .marquee{
    font-size:28px;
    font-weight:700;
    text-transform:uppercase;
    letter-spacing:3px;
    margin:8px 0 12px;
    display:flex;
    gap:8px;
    align-items:center;
    flex-wrap:wrap;
  }

  /* progress bar */
  .progress-wrap{
    margin-top:6px;
    width:100%;
    background:rgba(255,255,255,0.03);
    border-radius:10px;
    overflow:hidden;
    height:18px;
  }
  .progress{
    height:100%;
    width:0%;
    transition: width 600ms linear;
    background:linear-gradient(90deg,#00d4ff,#ff60b8);
    box-shadow:0 4px 18px rgba(255,96,184,0.15) inset;
  }

  /* meme tile area */
  .memes{
    display:grid;
    grid-template-columns: repeat(3, 1fr);
    gap:12px;
    margin-top:16px;
  }
  .meme{
    min-height:110px;
    background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.04));
    border-radius:10px;
    display:flex;
    align-items:center;
    justify-content:center;
    font-weight:700;
    color:rgba(255,255,255,0.95);
    text-align:center;
    font-size:14px;
    padding:8px;
    position:relative;
    overflow:hidden;
    cursor:grab;
    user-select:none;
  }

  /* pulsing meme labels */
  .meme::after{
    content:"T R O L L E D";
    position:absolute;
    bottom:6px;
    right:8px;
    font-size:9px;
    color:rgba(255,255,255,0.22);
    letter-spacing:2px;
  }

  /* floating chaos layers */
  .layer{
    position:absolute;
    inset:0;
    pointer-events:none;
    mix-blend-mode:screen;
    z-index:2;
  }

  /* emoji bombs */
  .emoji {
    position:fixed;
    font-size:24px;
    will-change: transform, opacity;
    pointer-events:none;
    user-select:none;
    z-index:9000;
    text-shadow: 0 2px 8px rgba(0,0,0,0.45);
  }

  /* flashing background stripes */
  .stripes{
    position:absolute;
    inset:-10%;
    background:repeating-linear-gradient(
      45deg,
      rgba(255,255,255,0.02) 0 10px,
      rgba(0,0,0,0.02) 10px 20px
    );
    transform: rotate(0deg);
    animation: spin 20s linear infinite;
    opacity:0.12;
    z-index:1;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  /* dancing mascot */
  .mascot{
    position:absolute;
    bottom:-40px;
    right:-40px;
    width:220px;
    height:220px;
    z-index:9001;
    pointer-events:none;
    transform-origin:center;
    animation: boing 3000ms infinite;
    filter: drop-shadow(0 20px 30px rgba(0,0,0,0.6));
    opacity:0.95;
  }
  @keyframes boing {
    0% { transform: translateY(0) rotate(-6deg) scale(0.98); }
    50% { transform: translateY(-24px) rotate(6deg) scale(1.02); }
    100% { transform: translateY(0) rotate(-6deg) scale(0.98); }
  }

  /* blinking border for the whole document */
  .flash {
    position:fixed;
    inset:0;
    pointer-events:none;
    border: 6px solid transparent;
    border-image: linear-gradient(45deg,#ff4d6d,#ffd600,#33d6a6,#6ea8ff) 1;
    animation: flashy 7s linear infinite;
    z-index:9999;
    opacity:0.12;
  }
  @keyframes flashy {
    0%{ transform:scale(1); opacity:0.12 }
    50%{ transform:scale(1.02); opacity:0.28 }
    100%{ transform:scale(1); opacity:0.12 }
  }

  /* panic button */
  .controls{
    position:fixed;
    left:20px;
    bottom:20px;
    z-index:10001;
    display:flex;
    gap:10px;
    align-items:center;
  }
  .btn{
    padding:12px 16px;
    border-radius:10px;
    border:0;
    cursor:pointer;
    font-weight:700;
    letter-spacing:1px;
    box-shadow:0 8px 20px rgba(0,0,0,0.45);
  }
  .btn.panic{
    background:linear-gradient(90deg,#ff3b3b,#ff7b3b);
    color:white;
  }
  .btn.calm{
    background:linear-gradient(90deg,#00d4ff,#2eb8ff);
    color:#012;
  }

  /* footer tip */
  footer{
    position:fixed;
    right:18px;
    bottom:8px;
    z-index:10002;
    font-size:11px;
    color:rgba(255,255,255,0.6);
    user-select:none;
  }

  /* tiny accessibility: reduce motion */
  @media (prefers-reduced-motion: reduce) {
    .mascot, .stripes, .flash { animation:none; transform:none; }
    .emoji { transition:none; }
  }
</style>
</head>
<body>
<header>
  <h1>WELCOME TO THE TROLL ZONE</h1>
  <p class="subtitle">This page contains numerous utterly ridiculous web shenanigans. Click around.</p>
</header>

<div id="stage" aria-hidden="false">
  <div class="card" role="region" aria-label="Troll console">
    <div class="toolbar">
      <span class="dot r"></span><span class="dot y"></span><span class="dot g"></span>
      <div style="flex:1"></div>
      <div style="font-size:11px; color:rgba(255,255,255,0.5)">TrollOS v.🦊</div>
    </div>

    <div class="marquee">
      <div id="marqueeText">INSTALLING HUMOR PACK...</div>
      <div id="tick" style="font-size:12px; color:rgba(255,255,255,0.32)">⏳</div>
    </div>

    <div class="progress-wrap" aria-hidden="true">
      <div id="progress" class="progress" role="progressbar" aria-valuemin="0" aria-valuemax="100"></div>
    </div>

    <div class="memes" id="memeGrid" aria-hidden="true">
      <div class="meme" data-meme="1">When you open a file and it's actually a meme</div>
      <div class="meme" data-meme="2">404: Respect not found</div>
      <div class="meme" data-meme="3">This site powered by chaos.exe</div>
      <div class="meme" data-meme="4">Press any key to continue (but don't)</div>
      <div class="meme" data-meme="5">You can't close me (probably)</div>
      <div class="meme" data-meme="6">Trolololololol</div>
    </div>

    <div style="margin-top:14px; display:flex; gap:8px; align-items:center;">
      <button id="spawnEmoji" class="btn calm" aria-pressed="false">Spawn Emoji Bomb</button>
      <button id="toggleMusic" class="btn calm">Play Silliness</button>
      <button id="invertBtn" class="btn calm">Invert Colors</button>
      <div style="flex:1"></div>
      <div style="font-size:12px; color:rgba(255,255,255,0.6);">Tip: Hit PANIC to return to normal.</div>
    </div>
  </div>
</div>

<!-- floating layers -->
<div class="layer stripes" aria-hidden="true"></div>
<div class="layer flash" aria-hidden="true"></div>

<!-- dancing mascot (SVG) -->
<svg class="mascot" viewBox="0 0 120 120" aria-hidden="true">
  <defs>
    <linearGradient id="g1" x1="0" x2="1">
      <stop offset="0" stop-color="#ff6b6b"/>
      <stop offset="1" stop-color="#ffd166"/>
    </linearGradient>
  </defs>
  <g transform="translate(10,8)">
    <ellipse cx="50" cy="56" rx="48" ry="34" fill="url(#g1)"></ellipse>
    <circle cx="32" cy="42" r="8" fill="#fff"></circle>
    <circle cx="68" cy="42" r="8" fill="#fff"></circle>
    <circle cx="32" cy="42" r="4" fill="#000"></circle>
    <circle cx="68" cy="42" r="4" fill="#000"></circle>
    <path d="M28,62 C40,76 60,76 72,62" stroke="#8b3a3a" stroke-width="4" fill="none" stroke-linecap="round"/>
  </g>
</svg>

<!-- controls -->
<div class="controls">
  <button id="panic" class="btn panic">PANIC — STOP EVERYTHING</button>
  <button id="calm" class="btn calm">Calm Down</button>
</div>

<footer>Made with ❤️ — Totally harmless. (Mostly.)</footer>

<!-- audio (user-initiated play only) -->
<audio id="sfx" preload="auto">
  <source src="" />
</audio>

<script>
/* ============== Utilities ============== */
const rnd = (min, max) => Math.floor(Math.random()*(max-min+1))+min;
const pick = arr => arr[rnd(0, arr.length-1)];

let running = true;

/* ============== Progress "installer" ============== */
const progressEl = document.getElementById('progress');
const marquee = document.getElementById('marqueeText');
const tick = document.getElementById('tick');

let progress = 0;
const fakeTasks = [
  "Warming up silliness",
  "Unleashing internet spirits",
  "Compiling dad jokes",
  "Feeding the memes",
  "Polishing sarcasm",
  "Summoning the emoji bomb",
  "Reticulating splines"
];

function startFakeProgress(){
  progress = 0;
  progressEl.style.width = '0%';
  marquee.textContent = "INITIALIZING...";
  let idx = 0;
  const t = setInterval(()=>{
    if(!running){ clearInterval(t); return; }
    progress += rnd(2,8);
    if(progress > 100) progress = 100;
    progressEl.style.width = progress + '%';
    marquee.textContent = fakeTasks[idx % fakeTasks.length] + ' — ' + progress + '%';
    tick.textContent = pick(['⏳','⚙️','🧪','🤣','🔥','🔧']) + ' ';
    idx++;
    if(progress >= 100){
      clearInterval(t);
      setTimeout(()=>{ prankReveal(); }, 700);
    }
  }, 600);
}
startFakeProgress();

/* ============== Meme drag + tiny wobble ============== */
document.querySelectorAll('.meme').forEach(el => {
  el.addEventListener('mousedown', e => {
    el.style.cursor = 'grabbing';
  });
  el.addEventListener('mouseup', e => {
    el.style.cursor = 'grab';
  });
  el.addEventListener('dragstart', e => {
    e.preventDefault();
  });

  // little wiggle on hover
  el.addEventListener('mouseenter', () => {
    el.animate([
      { transform: 'translateY(0) rotate(0deg)' },
      { transform: 'translateY(-6px) rotate(-2deg)' },
      { transform: 'translateY(0) rotate(2deg)' }
    ], { duration: 700, iterations: 2 });
  });
});

/* ============== Emoji shower ============== */
const emojis = ['😂','🤣','😈','👀','🔥','💥','🤡','🙃','🥸','🍕','🧋','💀','🎉','✨','🪲','🦋'];

function spawnEmoji(x,y,emoji){
  const node = document.createElement('div');
  node.className = 'emoji';
  node.style.left = (x||rnd(10,innerWidth-40)) + 'px';
  node.style.top = (y||-50) + 'px';
  node.style.fontSize = rnd(18,46)+'px';
  node.textContent = emoji || pick(emojis);
  document.body.appendChild(node);

  const duration = rnd(3000,7000);
  const horiz = rnd(-200,200);
  const rotate = rnd(-720,720);
  const start = performance.now();

  function frame(now){
    const t = Math.min(1, (now-start)/duration);
    node.style.transform = `translate(${Math.easeInOutQuad(t)*horiz}px, ${Math.easeOut(t)* (innerHeight + 120)}px) rotate(${rotate * t}deg)`;
    node.style.opacity = String(1 - t);
    if(t < 1 && running) requestAnimationFrame(frame);
    else node.remove();
  }
  requestAnimationFrame(frame);
}

Math.easeOut = t => 1 - Math.pow(1 - t, 3);
Math.easeInOutQuad = t => t<.5 ? 2*t*t : -1 + (4 - 2*t)*t;

/* spawn a few on load for dramatic effect */
for(let i=0;i<18;i++){
  setTimeout(()=>spawnEmoji(undefined, -50), i*160);
}

/* button to spawn a bomb of emoji */
document.getElementById('spawnEmoji').addEventListener('click', ()=>{
  for(let i=0;i<32;i++){
    setTimeout(()=>spawnEmoji(rnd(20,innerWidth-80), -40, pick(emojis)), i*40);
  }
});

/* ============== prank reveal ============== */
function prankReveal(){
  if(!running) return;
  // change the grid memes quickly and bounce
  const grid = document.getElementById('memeGrid');
  const texts = [
    "You just got trolled!",
    "¯\\_(ツ)_/¯",
    "No, seriously, you did.",
    "Hope you like confetti.",
    "Press PANIC if you regret this.",
    "This message will self-destruct in 0 seconds."
  ];
  Array.from(grid.children).forEach((c,i)=>{
    c.textContent = texts[i % texts.length];
    c.animate([
      { transform: 'scale(1) rotate(0deg)' },
      { transform: 'scale(1.08) rotate(2deg)' },
      { transform: 'scale(1) rotate(-2deg)' }
    ], { duration: 900, iterations: 3, delay: i*120 });
  });

  // cascade many emoji
  for(let i=0;i<120;i++){
    setTimeout(()=>spawnEmoji(rnd(0,innerWidth), -30), i*30);
  }

  // flashing marquee
  const m = document.getElementById('marqueeText');
  let flashCount = 0;
  const flash = setInterval(()=>{
    if(!running){ clearInterval(flash); return; }
    m.style.color = flashCount % 2 === 0 ? '#ffd166' : '#ff6b6b';
    flashCount++;
    if(flashCount > 30) clearInterval(flash);
  },120);
}

/* ============== Sound (user must click to allow audio) ============== */
const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
let isPlaying = false;
function playSillyTune(){
  if(isPlaying) return;
  isPlaying = true;
  const o = audioCtx.createOscillator();
  const g = audioCtx.createGain();
  o.type = 'sawtooth';
  o.frequency.setValueAtTime(440, audioCtx.currentTime);
  o.connect(g);
  g.connect(audioCtx.destination);
  g.gain.setValueAtTime(0.0001, audioCtx.currentTime);
  g.gain.exponentialRampToValueAtTime(0.05, audioCtx.currentTime + 0.02);
  o.start();
  // playful arpeggio
  let t = audioCtx.currentTime;
  const notes = [440, 660, 880, 1100, 880, 660, 440];
  notes.forEach((n,i)=> {
    o.frequency.setValueAtTime(n, t + i*0.12);
  });
  setTimeout(()=>{
    g.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + 0.6);
    setTimeout(()=>{ o.stop(); isPlaying=false; },800);
  }, 1200);
}
document.getElementById('toggleMusic').addEventListener('click', ()=>{
  try{ audioCtx.resume().then(()=>playSillyTune()); } catch(e){}
});

/* ============== invert colors button ============== */
let inverted = false;
document.getElementById('invertBtn').addEventListener('click', ()=>{
  inverted = !inverted;
  if(inverted) document.documentElement.style.filter = 'invert(1) hue-rotate(180deg)';
  else document.documentElement.style.filter = '';
});

/* ============== Panic / Calm ============== */
function stopAll(){
  running = false;
  progressEl.style.width = '0%';
  marquee.textContent = 'STOPPED.';
  // remove emojis quickly
  document.querySelectorAll('.emoji').forEach(n => n.remove());
  // restore any filters
  document.documentElement.style.filter = '';
  document.querySelectorAll('.meme').forEach(m=>{
    m.style.transform = '';
  });
}
function calm(){
  running = true;
  marquee.textContent = 'Calm mode — minimal trolling';
  startFakeProgress(); // resume light activity
}

document.getElementById('panic').addEventListener('click', ()=>{ stopAll(); });
document.getElementById('calm').addEventListener('click', ()=>{ calm(); });

/* ============== Cursor antics (mild) ============== */
let wiggle = false;
document.addEventListener('mousemove', e => {
  if(!running) return;
  if(rnd(0,600) > 598 && !wiggle){
    wiggle = true;
    const x = e.clientX, y = e.clientY;
    const notice = document.createElement('div');
    notice.textContent = pick(['Hehe','Gotcha','👀','Wow','Impossible!','Nope']);
    notice.style.position='fixed';
    notice.style.left=(x+10)+'px';
    notice.style.top=(y+10)+'px';
    notice.style.padding='6px 10px';
    notice.style.borderRadius='8px';
    notice.style.fontWeight='700';
    notice.style.background='rgba(255,255,255,0.06)';
    notice.style.backdropFilter='blur(4px)';
    notice.style.pointerEvents='none';
    notice.style.zIndex=99999;
    document.body.appendChild(notice);
    notice.animate([
      { transform:'translateY(0)', opacity:1 },
      { transform:'translateY(-40px)', opacity:0 }
    ], { duration:900, easing:'ease-out' });
    setTimeout(()=>{ notice.remove(); wiggle=false; },1000);
  }
});

/* ============== Prevent annoying things: avoid infinite alert loops ============== */
/* intentionally left blank: we will not use alert loops (browsers block them). */

/* ============== Cleanup before unload (so it's not too malicious) ============== */
window.addEventListener('beforeunload', ()=> {
  // quickly try to stop any audio if playing
  try { if(audioCtx && audioCtx.state !== 'closed') audioCtx.suspend(); } catch(e){}
});

/* ============== Extra: Konami-ish mini easter egg ============== */
const konami = [38,38,40,40,37,39,37,39,66,65];
let kpos = 0;
window.addEventListener('keydown', (e)=>{
  if(e.keyCode === konami[kpos]){
    kpos++;
    if(kpos === konami.length){
      kpos = 0;
      // full-on confetti drop
      for(let i=0;i<180;i++) setTimeout(()=>spawnEmoji(rnd(0,innerWidth), -20, pick(['🎉','✨','🎊','🪩','🍕','🍩'])), i*12);
    }
  } else {
    kpos = 0;
  }
});

</script>
</body>
</html>
